variables:
  ReleaseArguments: ' --filter "TestCategory!=Quarantine" --verbosity normal /p:IsNightly=true /p:GeneratePackageOnBuild=true '
  VmImage: vs2017-win2016 # https://docs.microsoft.com/en-us/azure/devops/pipelines/agents/hosted?view=azure-devops


jobs: 
- template:  templates/build-test.yml
  parameters:
    BuildConfiguration: Release
    Arguments: $(ReleaseArguments)
    VmImage: $(VmImage)
  